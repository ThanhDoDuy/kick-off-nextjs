import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["que-thanh-garden.s3.amazonaws.com", "que-thanh-garden.s3.ap-southeast-2.amazonaws.com"],
  },
};

export default nextConfig;
